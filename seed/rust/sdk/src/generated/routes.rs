//! Every modelled route as public data. Generated; the seed ships the table empty.

use crate::route::Route;

/// Every route the model declares, in `operationId` order.
pub static ROUTES: &[&Route] = &[];
